# dcs-decoder/src/build

This folder contains files generated by emscripten. These are generated by running the [`build.sh`](../../build.sh) script.

These files are checked in to git to simplify the github actions build process, and because they're not too large.
